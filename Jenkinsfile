pipeline {
     agent any
     stages {
          stage('Build') {
              steps {
                  sh 'echo "Hello World"'
                    }
              stage('Lint HTML') {
                   sh 'echo "just kidding"'
                    }
         }
     }
}
